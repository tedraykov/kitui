import type { FormControlOptions } from "$lib";

const defaultOptions: FormControlOptions = {
    defaultProps: {
        element: 'div'
    },
    variants: [{
        props: {},
        styles: {
            root: 'w-fit'
        }
    }]
};

export default defaultOptions;
